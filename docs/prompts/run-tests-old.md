Run tests and if need fix the code or tests. Do this repeatedly till all tests pass in:

- scripts/shell/tests/run-all-tests.sh --e2e

Run the app in a browser you can control then run manual uats in docs/tech/web/user-acceptance-tests.md and if need implement fixes. For each test, first remove the pass mark. Then mark each as passed only after each passes. Do this repeatedly till all tests pass.

For each fix, file a bug report in docs/bug-reports/yyyy/MM/dd/yyyy-MM-dd-<serial>-<name>.md if appropriate.
