# Step 21.5 Remaining Work

**Parent plan:** [000-plan.md](./000-plan.md)
**Step plan:** [001-21.5.md](./001-21.5.md)
**Date:** 2026-06-07

This document captures the concrete frontend inventory and open work for Step `21.5` without expanding the scope of the main step plan.

## Current Frontend Route And File Map

### Current route tree

The current route registration lives in `apps/web/src/app/router.tsx`.

Primary and manage routes already aligned to the target shell:

- `/mission-control` -> `apps/web/src/features/mission-control/MissionControlPage.tsx`
- `/agents` -> `apps/web/src/features/agents/AgentsPage.tsx`
- `/agents/:id` -> `apps/web/src/features/agents/AgentDetailPage.tsx`
- `/agents/:agentId/capabilities/:family` -> `apps/web/src/features/agents/AgentCapabilityPage.tsx`
- `/skills` -> `apps/web/src/features/skills/SkillsPage.tsx`
- `/connections` -> `apps/web/src/features/connections/ConnectionsPage.tsx`
- `/credentials` -> `apps/web/src/features/credentials/CredentialsPage.tsx`
- `/activity` -> `apps/web/src/features/activity/ActivityFeedPage.tsx`
- `/outcomes` -> `apps/web/src/features/outcomes/OutcomeBoardPage.tsx`
- `/billing` -> `apps/web/src/features/billing/BillingPage.tsx`
- `/settings` -> `apps/web/src/features/settings/SettingsPage.tsx`

Advanced or still-transitional routes:

- `/bots` -> `apps/web/src/features/bots/BotsPage.tsx`
- `/bots/:id` -> `apps/web/src/features/instances/detail/InstanceDetailPage.tsx`
- `/venue-accounts` -> `apps/web/src/features/venue-accounts/VenueAccountsPage.tsx`
- `/exposure` -> `apps/web/src/features/exposure/ExposurePage.tsx`

What this means for the step:

- the route skeleton is already close to the desired information architecture
- the remaining work is mostly truthful read models, bridge surfaces, and progressive disclosure rather than wholesale route invention
- `/exposure`, `/bots`, and `/venue-accounts` should stay explicitly advanced

## Working File Inventory

### Navigation shell files

- `apps/web/src/app/layout/Sidebar.tsx`
- `apps/web/src/app/layout/RootLayout.tsx`

Current grouping is already mostly correct:

- primary: mission control, skills, activity, outcomes
- manage: agents, connections, credentials, billing, settings
- advanced: bots, trading setup, exposure

Concrete follow-up:

- keep `Connections` and `Credentials` in the primary setup journey
- keep `Bots` and `Trading setup` demoted
- ensure `Exposure` is treated as advanced infrastructure rather than a top-level core destination

### Mission-control slice

- `apps/web/src/features/mission-control/MissionControlPage.tsx`
- `apps/web/src/features/agents/AgentSummaryCard.tsx`
- `apps/web/src/lib/api-client.ts`

Current state to preserve or replace intentionally:

- the page already routes its primary CTA to `/agents?create=1`
- summary counts already come from `agents.list()`
- recent activity already comes from `dashboard.activity()`
- capability readiness is surfaced through `AgentSummaryCard` rather than a dashboard-specific bot summary contract

Remaining work:

- keep mission control on agent-first truth and avoid reintroducing any bot-centric overview contract
- decide whether a dedicated capability-aware overview contract is still needed or whether `agents.list()` plus per-agent readiness is sufficient for this step
- watch for scaling or UX issues from the current per-card readiness queries before widening the shell further

### Agent list, detail, and create/edit slice

- `apps/web/src/features/agents/AgentsPage.tsx`
- `apps/web/src/features/agents/AgentDetailPage.tsx`
- `apps/web/src/features/agents/EditAgentModal.tsx`
- `apps/web/src/features/agents/agent-display.ts`
- `apps/web/src/features/agents/AgentSummaryCard.tsx`
- `apps/web/src/features/agents/AgentCapabilityPage.tsx`

Observed gaps:

- execution mode is already surfaced in create, review, list, and detail
- capability setup is already injected conditionally based on the selected preset's capability families
- the remaining gap is that the flow is still preset-driven rather than explicit-skill-driven
- the agent detail page is already the bridge into capability workspaces, but the trading capability page still routes next steps through legacy trading setup surfaces

### Platform-resource slice

- `apps/web/src/features/connections/ConnectionsPage.tsx`
- `apps/web/src/features/credentials/CredentialsPage.tsx`

Current state:

- `ConnectionsPage.tsx` is already close to the desired platform-resource framing and accepts free-text providers
- `CredentialsPage.tsx` still carries a hard-coded provider template model and needs to move to provider-neutral language with optional presets

Minimum expectations:

- provider input remains open-ended
- credential creation is not framed as a fixed venue list
- users can manage existing platform resources rather than only create/list them

### Advanced capability and trading infrastructure slice

- `apps/web/src/features/bots/BotsPage.tsx`
- `apps/web/src/features/instances/detail/InstanceDetailPage.tsx`
- `apps/web/src/features/venue-accounts/VenueAccountsPage.tsx`
- `apps/web/src/features/trading-instances/InstancesPage.tsx`
- `apps/web/src/features/exposure/ExposurePage.tsx`

Use them as advanced destinations only.

Decision pressure:

- `venue-accounts` should either remain explicitly temporary advanced infrastructure or be replaced later by a bindings UI
- no new primary flow should route users into these pages before they have entered trading from an agent surface

## Recommended 21.5 Work Order

1. `apps/web/src/features/agents/AgentCapabilityPage.tsx`
2. `apps/web/src/features/credentials/CredentialsPage.tsx`
3. `apps/web/src/features/agents/EditAgentModal.tsx`
4. `apps/web/src/features/skills/SkillsPage.tsx`
5. `apps/web/src/app/layout/Sidebar.tsx`
6. any advanced trading page touched only to relabel or further demote it
7. `apps/web/src/features/mission-control/MissionControlPage.tsx` only if the shell still needs a consolidated capability-aware overview pass after the capability and credentials cleanup

## Explicit Remaining Gaps

- capability next steps still route trading setup through `/venue-accounts` rather than a bindings-first path
- credentials still lean on trading-heavy secret templates and backend `venue` naming even though the UI has become provider-first
- create/edit flows still depend on presets rather than explicit skill selection
- advanced trading infrastructure pages still exist without a fully explicit bindings-first replacement path
