# Operator configuration — self-documenting reference.
# Resolution: default.yaml → config/{NODE_ENV}.yaml → environment variables.

# UNCOMMENT IF YOU WANT TO USE THIS INSTEAD OF OLLAMA
llm:
  provider: openrouter                  # LLM provider: openrouter | openai | anthropic | ollama | deepseek
  model: deepseek/deepseek-v4-pro       # Model for non-agent API calls (e.g. /ai/generate-config). Agent model is set in agent config or user AI settings.
  baseUrl: https://openrouter.ai/api/v1 # DeepSeek API base URL (OpenAI-compatible)

# UNCOMMENT IF YOU WANT TO USE THIS INSTEAD OF OLLAMA
agentRuntime:
  llm:
    modelDefaults:
      provider: openrouter                    # suggested provider in forms
      lightModel: deepseek/deepseek-v4-flash  # suggested economy (scout) model
      heavyModel: deepseek/deepseek-v4-pro    # suggested premium (judge) model

platformAssessor:
  enabled: true
  minReviewIntervalMs: 300000  # 5 min — operator floor for agent reviewIntervalMs
  cacheFreshnessMs: 300000  # 5 min — lookup and artifact expiry
  llm:
    baseUrl: http://host.docker.internal:11434/v1  # explicit for dev — ollama runs on host

# Alerting — dispatches journal events to Telegram channels.
alerts:
  email:
    brandImageUrl: http://localhost:8080/brand/compact-mark.png

# ── Infrastructure Services ────────────────────────────────────────────────
# Declarative service URL map. Each key is a Nomad service name.
# When url is non-empty, the worker uses it directly (Docker Compose path).
# When url is empty and runtimeBackend is 'nomad', the worker resolves the
# address from the Nomad service catalog at startup.
services:
  browser-pool:
    url: "http://browser-pool:3000"   # Docker Compose; leave empty for Nomad discovery


