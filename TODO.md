# TODO

Some of these may no longer be valid (they may have been done/implemented). 

- [ ] high. Override tickets are still not fully scoped to the bot they were issued for. The schema and docstring say overrides are scoped to actor, bot, venue account, network, and token in token-safety-overrides.ts:8, and the row model stores botId in token-safety-override-repository.ts:22. But lookup still only keys on actor, venue account, network, and token in token-safety-override-repository.ts:57, and the adapter only passes those fields in token-safety-adapter.ts:57. That means the same actor can still reuse an override across multiple bots on the same venue account for the same token.
What to change: modify the repository and adapter so fetch and consume are also scoped by botId, with explicit handling for null botId.
Dependencies: none.
Risk or open question: decide whether a null botId means actor-wide scope or only requests that also have null botId.
Testing: unit test the repository matching rules, and integration test the adapter path with bot A vs bot B on the same venue account. No visual verification needed.

- [ ] high. Swap asset identifiers are still inconsistent across the new safety path, so the runtime can reject valid swap configs or miscompute notional depending on whether configs use symbols or addresses. The config schema still treats swapAssets as generic identifiers and existing tests continue to use symbol values like SOL and USDC in schema.ts:832, schema.test.ts:25, and decision-intake.test.ts:220. But the worker now forwards baseAsset as a token address in index.ts:826, while the notional estimator still assumes quoteAsset is a symbol and checks it against a stablecoin symbol set in decision-intake.ts:341 and decision-intake.ts:356. Those two assumptions cannot both be true for the same field model.
What to change: normalize swap config at the boundary into explicit symbol and address fields, or introduce separate baseTokenAddress and quoteTokenAddress fields and keep symbol fields for pricing logic.
Dependencies: this touches config schema, worker wiring, and the engine notional helper.
Risk or open question: existing persisted bot configs may need a migration or backward-compat normalization.
Testing: unit test config parsing and notional estimation, plus integration test the worker safety resolution path for both Jupiter and 1inch style configs. No visual verification needed.

- [ ] high - apps/web/src/lib/ui.tsx and apps/web/src/styles.css should respect prefers-reduced-motion. The new pulse animation is always on for live statuses, so users who opt out of motion still get an infinite blink. What to change: gate the animation behind a reduced-motion media query or disable the pulse for those users. Dependencies: none. Risk/open question: whether you want the pulse on only some live statuses or all of them. Testing: visual verification plus a manual accessibility check.

- [ ] Medium — Modify the market-data observability path so the “Waits” column is either real or removed. What to change: the UI currently renders wait counts in AdminMarketDataSection.tsx:232 and AdminMarketDataSection.tsx:297, and the counter writer exists in provider-counters.ts:80, but the request-gate contract still discards wait information in types.ts:27 and rate-limiter.ts:321. Dependencies: do this before relying on the admin market-data table for rate-limit tuning. Risk/open question: if you keep the column as-is, operators will read a permanent 0 as “no waits happened” when the system simply cannot observe them yet. Tests: unit-test the rate limiter surface once it returns wait metadata; integration-test that a delayed acquire increments rateLimitWaitCount; visually verify the admin providers table shows non-zero waits under a forced delayed-budget scenario.

- [ ] Medium. session_started usage events are being overcounted on recovery, and they are not idempotent. In agent-session-manager.ts:346-362, startedFromNonRunning includes unhealthy, so a heartbeat that merely recovers an already-running session logs a fresh session_started event. The insert path in agent-repository.ts:315-344 also has no dedupe guard, so concurrent first heartbeats can double-insert for the same session. What to change: only emit this event on a true session start transition, or rename/use a different recovery event if unhealthy recovery is intentionally tracked; make the persistence idempotent by session/skill/event type. Dependencies: depends on the intended meaning of session_started. Risks/open questions: if recoveries should count separately, the current event name and metrics queries are misleading. Testing: integration-test starting -> running, unhealthy -> running, and duplicate first-heartbeat races; no visual verification needed.

- [ ] Medium - Add one end-to-end test for the new admin flow in admin.test.ts:149 and bots.test.ts:93. What to change: exercise the real auth plugin and database once for POST /admin/users/:id/promote or the admin bot bypass path, rather than only mocked request decoration. Dependency: the auth plugin must be able to hydrate request.isAdmin in the test harness. Risk/open question: slightly slower tests, but it verifies the actual request plumbing that the feature depends on.

- [ ] Low — Use the server-side pagination hooks that already exist instead of always loading full user and webhook datasets into the admin page. What to change: AdminPage.tsx:28 and AdminPage.tsx:33 still call the unpaginated adminApi.users() and adminApi.webhooks() even though the API client and routes now support limit and offset; the section components then paginate locally. Dependencies: none, but it is easiest if the page owns page, limit, and offset state and passes only the current slice plus total metadata down to the section components. Risk/open question: this is not a correctness bug today, but on a larger tenant it will make the admin page do unnecessary work and makes the backend pagination support effectively dead code. Tests: unit-test the query-key changes if you add page state; integration-test the route parameters from the client; visually verify that moving between pages fetches and renders the correct rows rather than only slicing an in-memory array.

- [ ] Low. maxPublishedSkills is declared but currently dead config. The entitlement is added in schema.ts:248-252, but the skills routes only enforce pricing and privacy flags in skills.ts:575-586 and skills.ts:777-778. A plan can therefore set maxPublishedSkills and still publish unlimited skills. What to change: either enforce this cap in create/publish/update transitions, or remove the field until the limit is implemented. Dependencies: decide whether the cap counts only published, or also delisted/archived rows. Risks/open questions: shipping unused operator config is a footgun because it looks supported but has no effect. Testing: unit-test entitlement resolution and integration-test route behavior once the cap is defined; no visual verification needed.

- [ ] Low. The web publish client still advertises a payload the API no longer accepts. api-client.ts:337-340 sends optional priceCents, while skills.ts:53-55 only accepts revisionId. Because the server schema strips unknown keys, this fails silently instead of surfacing a contract error. What to change: remove priceCents from the client method, or restore explicit server support if publish-time pricing is still intended. Dependencies: align on the intended publish contract first. Risks/open questions: this is low severity today, but it will create hard-to-debug UI regressions the moment a caller tries to rely on that payload. Testing: unit-test the client contract or add a narrow API integration test; visual verification only matters if the UI later exposes publish-time pricing.

- [ ] Low - Harden the bootstrap path in seed-admin.ts:62. What to change: remove the remaining read-before-write race window, or at least catch duplicate-email conflicts explicitly inside the transaction. Dependency: none. Risk/open question: if this script is only ever run manually once, the race is low impact; if it can be run concurrently in automation, the current pre-check still leaves a failure path.

- [ ] Medium. agent-activity-feed.test.ts:61, agent-activity-feed.test.ts:73, agent-activity-feed.test.ts:135, agent-activity-feed.test.ts:178, agent-activity-feed.test.ts:219, agent-activity-feed.test.ts:265, agent-activity-feed.test.ts:305, agent-activity-feed.test.ts:336. The test file repeatedly does await import('./agents.js') inside every case. In the current suite this is already slow enough that the first test intermittently times out when the whole file is run, even though it passes in isolation. That makes the new feed coverage flaky and weakens confidence in the implementation. Hoist the import into beforeAll or module scope so route registration cost is paid once per file instead of once per test.

- [ ] Medium — .env.example:36: the new LLM provider comment is already stale relative to the actual provider catalog. The example says only openrouter, openai, anthropic, and ollama are valid, but the runtime supports additional providers including together, fireworks, mistral, cohere, and google in llm-models.ts:3. This will steer operators toward an artificially smaller config surface and makes the example file disagree with the real validation/catalog source of truth.
What to change: update the comment to either list the full supported set or avoid enumerating providers there and point readers to the catalog/config docs instead.
Change type: modify documentation comment.
Dependencies: none.
Risks / open questions: listing providers inline will keep drifting unless you treat one file as the source of truth. The safer long-term option is to reference the catalog rather than duplicate it.
Test guidance: no unit or integration test needed; this is a documentation consistency fix. No visual verification needed.

- [ ] Low — MissionControlPage.tsx:15: the new exported MissionControlSetupForm wrapper appears to exist primarily for test convenience, which widens the production module API and adds an extra indirection without changing runtime behavior. The production page still depends on the wrapper being used correctly at MissionControlPage.tsx:217, so this is a maintainability tradeoff rather than a functional bug.
What to change: either keep the wrapper but treat it as an intentional local abstraction with a short comment explaining why it exists, or move the wiring test up to the page/component boundary and collapse the wrapper back into the inline usage.
Change type: modify structure or add clarifying comment.
Dependencies: none.
Risks / open questions: if the wrapper remains purely as test scaffolding, future readers may not know whether it is a real UI abstraction or just a seam for tests.
Test guidance: if you remove the wrapper, replace it with a narrow component/integration test that proves the Mission Control setup path renders ProviderSetupForm in trading mode. No visual verification needed.

- [ ] Low. agent-broker.test.ts:1457. The new audit-only broker test for agent.llm.completed includes terminatedByLimit: false, but that field is not part of the actual LlmCompletedPayloadSchema. Because Zod strips unknown keys by default, the test still passes, which means it is not actually guarding the real contract and can mask payload drift. Remove the unsupported field from the fixture, or make the test assert the accepted schema shape explicitly.

- [ ] medium - apps/api/src/routes/agent-activity-feed.test.ts and apps/api/src/routes/dashboard-agent-activity.test.ts do not actually prove the new SQL filtering works. The mocked DB returns canned rows regardless of the where clause, so these tests can still pass if notInArray(...) is removed or broken. What to change: add an integration-style check against a real DB, or make the mock assert the predicate it receives; also keep one route case for the legacy agent.heartbeat format since it is part of the suppression list. Dependencies: none. Risk/open question: whether you prefer a full integration test or a stricter mock.

- [ ] low - apps/api/src/routes/agent-activity-mapper.ts now owns a feed-policy constant, and both route handlers still do a second in-memory suppression check after the SQL filter. That is workable, but it couples policy to mapping and duplicates logic that can drift. What to change: either move the suppression list into a shared feed-policy module or drop the redundant row-level guard once the query-layer tests are stronger. Dependencies: item 2 if you want to rely on query-level coverage. Testing: unit test for the extracted helper if you split it out.

- [ ] Medium — Modify wake coalescing semantics in monitor.ts:589 and monitor.ts:668. The new bucket state keeps the first event’s reason/source/context forever, even when later events are coalesced into the same wake. That means a later regime-change or discovery event can be present in eventIds while the immediate tick still renders the earlier watch summary, which is semantically misleading relative to the new “triggering event” goal. Change: either make coalescing last-wins for primaryReason/primarySource/primaryContext, or define and implement an explicit priority order for mixed-source buckets. Dependency: none. Risk/open question: if first-wins is intentional, it should be documented as the contract and covered by tests; otherwise this is a behavior bug. Test scope: unit test in the monitor or wake-scheduler for heterogeneous coalescing; an integration test is optional, no visual verification needed.

- [ ] Low — Add direct runtime coverage for the typed reminder path in runtime-composition.ts:946. All reminder runtime tests still exercise only the legacy prefix shape at runtime-composition.test.ts:521, runtime-composition.test.ts:537, and runtime-composition.test.ts:557. The new producer-side test at reminder-coordinator.test.ts:66 is also mislabeled: it claims to prove decoding “without the legacy prefix”, but the producer still emits prefixed wakeId and reason at reminder-coordinator.ts:79 and reminder-coordinator.ts:80. Change: add a runtime-composition test with source set to reminder, non-prefixed wakeId/reason, and context-provided reminderId/message; also rename or tighten the reminder-coordinator test so it matches what it actually proves. Dependency: none. Risk/open question: a future cleanup that removes the legacy prefixes could break typed reminder handling without any failing test. Test scope: unit test only, no integration or visual verification needed.

- [ ] apps/api/src/plugins/auth.integration.test.ts only proves that GET /events does not hit the header-auth 401 path. It does not exercise the actual WebSocket upgrade flow or the ?token= validation path, so it can pass even if the browser-facing connection still breaks. What to change: move this regression into the existing websocket coverage in apps/api/src/routes/events.test.ts or add an explicit handshake assertion there. Dependency: none. Risk/open question: if the intent is only to verify middleware bypass, keep the current test but add websocket-level coverage as the real regression guard. Test type: integration.

- [ ] apps/worker/src/agents/agent-message-broker.ts now hard-codes the planned-shutdown reasons in a local set, while the runtime emits the same lifecycle reasons from apps/worker/src/agent.ts and apps/worker/src/agent.ts. That duplication is fragile: if another orderly shutdown reason gets added later, one path can classify it as planned while the other does not. What to change: centralize the shutdown-reason constants in a shared domain helper or enum and reuse it in both places. Dependency: none. Risk/open question: if SIGTERM/SIGINT are intentionally runtime-internal rather than domain-level reasons, document that distinction so the two code paths do not drift. Test type: unit test the shared helper; one integration test for a planned shutdown reason is enough.

- [ ] The message feed still conflates primary feed persistence with external-channel delivery in agent-message-broker.ts:383 and AgentDetailPage.tsx:50. The broker persists the message before Telegram/email delivery, but the UI still treats deliveryStatus as the overall badge state, so a feed-only message can still appear failed even though the user received it in the app. What to change: add an explicit feed-delivery/overall-delivery concept, or rename and re-scope deliveryStatus so consumers do not read it as the whole story. Dependencies: this touches the broker persistence contract and the web API/client model. Risk or open question: if deliveryStatus is intentionally Telegram-only, the UI should stop using it as the top-level status label and show feed delivery separately. Testing: unit test the status derivation, integration test the messages API for feed-only cases, and visually verify the badge for a message with no Telegram destination and no email request.

- [ ] Reminder wakes are now deleted after firing in reminder-coordinator.ts:85, but there is no durable fired marker or idempotency guard if the delete fails. A successful wake followed by a Redis error on hdel will leave the reminder in place and cause it to fire again on the next poll. What to change: make the reminder transition atomic or keep a fired marker until the wake is durably acknowledged, so repeated polls cannot double-fire the same reminder. Dependencies: this depends on the chosen reminder delivery semantics, especially whether you want at-least-once with downstream dedupe or strict one-shot behavior. Risk or open question: if duplicate wake delivery is acceptable, that should be documented; otherwise this needs a more robust state transition than plain delete-after-emit. Testing: unit test the Redis error path and integration test a retry/poll cycle to confirm the reminder cannot fire twice.

- [ ] modify the cooldown timestamp update in monitor.ts:579. The code captures now before the external emitAgentMarketWake() await, then stores that stale timestamp in lastWakeKey at monitor.ts:592. If publish blocks for several seconds, the cooldown effectively starts early, so a follow-up wake for the same agent can become eligible sooner than intended right after the send completes. This is more visible now that publish happens outside the lock and can legitimately take a while. What to change: record the cooldown anchor with a fresh Date.now() after the publish returns, per emitted wake. Dependencies: none. Risk/open question: none beyond slightly lengthening cooldown to match actual emission time, which is the correct behavior. Test scope: unit-test this in wake-scheduler.test.ts with a deferred publish and assert the second wake is still suppressed until wakeCooldownMs after the publish resolves.

- [ ] coordinator.ts:108, coordinator.ts:188, monitor.ts:118, monitor.ts:474: the new stop and stopped checks reduce the race, but they do not fully fence in-flight work. A refresh or flush that has already passed the guard can still complete a Redis write or wake emission after leadership is lost. What to change: capture a lease or generation token at the start of each cycle and re-check it immediately before every write or publish, or make the write conditional on the token still matching. Dependency: none, but this should be fixed before treating the coordinator and monitor as fully single-leader safe. Risk/open question: the current tests cover stop behavior, not a leader-loss mid-await race. Test: an integration test that flips leadership during discovery/regime evaluation or wake flush and asserts no late writes or emissions.

- [ ] agent.ts:435, agent.ts:800, agent.ts:831, agent.ts:1526: the dedicated wake consumer group fixes the bootstrap gap, but it still consumes the same outbound stream and acks every message, not just wake signals. That doubles stream work for heartbeats and context snapshots and keeps wake handling tied to a one-second blocking loop. What to change: either move wake signals onto a dedicated stream or narrow this consumer so it does not parse and ack the full agent stream. Dependency: none. Risk/open question: if the shared-stream design is intentional, document the extra consumer overhead and the wake latency ceiling. Test: an integration/load test that runs normal outbound traffic plus wake messages and verifies wake latency stays within the intended bound.

- [ ] The “hide unavailable market-data tools” fix only removes them from requiredTools, but the system prompt still tells the model to use them. The startup filter in agent.ts:269 rewrites skill.requiredTools, but buildSystemPrompt still injects each skill’s static instructions, and TRADING_SKILL still explicitly says to use check_regime and search_tokens. What to change: apply the same local capability filtering to prompt-visible instructions, or stop rendering raw skill instructions and instead generate tool guidance from the visible tool set. Dependencies: none. Risk/open question: with market data disabled, the agent can still waste turns trying to reason about tools it has been told to use even though they no longer appear in the available-tools list. Testing: unit test buildSystemPrompt() with market data disabled and assert those tool names do not appear anywhere in the prompt.

- [] Runtime config updates will undo the local tool filtering and re-expose unavailable tools. updateRuntimeDescriptor replaces the descriptor wholesale when an agent.runtime.config_update arrives, and the current “strip market-data tools” logic only runs once at startup in agent.ts:269. What to change: centralize a “local runtime normalization” step that enforces environment-dependent capability constraints, and invoke it both after initial descriptor creation and after every runtime descriptor update. Dependencies: this should probably be implemented together with the prompt fix above so tools and instructions stay consistent. Risk/open question: after a config refresh, allowedTools() and the prompt can drift back to advertising check_regime/search_tokens even though the process still lacks market-data config. Testing: unit/integration test applyRuntimeMessage() with market data disabled and assert the tools remain hidden after a descriptor update.

 - [ ] live tool-policy refresh is still incomplete because only code_execute actually consults the refreshed capability policy. The visible tool set is still derived solely from skills in agent.ts:183, and brokered actions like artifact publish, decision submit, and bot creation go straight through in agent.ts:400, agent.ts:412, and agent.ts:426. Only code_execute checks capabilityEngine in agent.ts:436. So if toolPolicy disables decision_submit or manage_bot mid-session, the container will still emit those actions until some downstream broker rejects them. That leaves the new runtime descriptor refresh only partially authoritative. We need to make every brokered tool consult toolPolicy inside the container. It is a large behavioral change which was done earlier because the platform broker is still the primary enforcement point there. 

- [ ] trading.ts:277 still returns a nondeterministic bindingId when an agent has multiple active trading grants. The query has no ordering, and the route uses rows.find(...) at trading.ts:305, so whichever active row happens to come back first becomes the reported binding. That makes the family-specific readiness endpoint unstable even after the aggregate endpoint was fixed. I would order the rows by grantedAt desc, id desc or reuse the same deterministic selector for active grants too. This needs an integration-style route test that seeds two active grants and asserts the chosen bindingId.

- [ ] AgentDetailPage.tsx:21 compares event.botId to the page’s id, but on this page id is the agent ID, not a bot ID. The UserEvent contract in useEventStream.ts:15 confirms that bot.status events only carry botId, so this branch now never matches unless IDs collide by accident. The result is that agent activity stops refreshing on bot status changes entirely. Either the event payload needs an agentId, or this page needs a different signal for “a bot under this agent changed.” This should be covered by a component/event-handler test or a small integration test around the event stream hook.

- [ ] index.ts:701 still leaves instanceUserIds populated on the bot crash path. Entries are created at index.ts:712 and cleaned up for start-failure/stop at index.ts:323, index.ts:337, and index.ts:717, but not when onCrashed runs. In a long-lived worker, repeated crashes will grow this map indefinitely and retain stale user-routing metadata until process restart. The cleanup should happen in the crash path too, or be centralized with the other runtime teardown.

- [ ] The agent-journal scope was changed in exports.ts:590 and exports.ts:736, but there is still no direct route test for GET /agents/:id/export/journal in exports.test.ts:453. The agent export tests cover trades/config/bundle, not the journal endpoint itself. Since this area was just corrected from agent-id scoping to managed-bot scoping, a focused test would be the cheapest guard against regressing that behavior again.

- [ ] blueprints.ts:226, blueprints.ts:235, blueprints.ts:266, blueprints.ts:277, blueprints.ts:321, and blueprints.ts:333 still leave blueprint mutations only partially serialized. PUT /blueprints/:id now takes an advisory lock, but DELETE, publish, and unpublish do not participate in that same lock. So a concurrent delete or visibility change can still interleave with the locked update path, and PUT can return 200 with an empty body or stale post-update assumptions if the row disappears or changes mid-transaction. If the intent is “one blueprint mutation at a time,” every mutating endpoint for the same blueprint id needs to use the same lock key.

- [ ] schemas.ts:8, schemas.ts:12, bots.ts:27, and bots.ts:29 still accept an ambiguous POST /bots body containing both blueprintId and legacy inline config. The route silently prefers blueprintId and ignores config, which is better than the old configOverrides hole, but it is still a silent conflict in the public contract. This should be rejected at validation time so callers do not think their inline config was applied when it was discarded.

- [ ] bots.ts and bots.ts still break `/bots/:id/sessions` pagination. The route now builds session pairs correctly, but it fetches the first `maxEvents` lifecycle events in ascending order before pairing and reversing. Once a bot has more than `(limit + offset) * 2 + 2` lifecycle events, page 1 stops reflecting the newest sessions and instead returns the newest sessions from the oldest slice of history. This is a real data-loss bug for any bot with longer history. The current stub test at bots.test.ts only checks that `sessions` exists, so it would not catch this.

- [ ] agents.ts and agents.ts return different response shapes for `/agents/:id/trades`. When an agent has no managed bots, the route returns `{ agentId, trades: [] }`; otherwise it returns `{ agentId, trades, limit, offset }`. That makes the contract data-dependent and forces clients to special-case the empty state. The matching test at agents.test.ts only asserts that `trades` is an array, so this drift is currently untested.

- [ ] agents.ts still imports `PgJournal` and `FillRepository`, but neither is used anymore. It is not a runtime defect, but it obscures the actual dependencies in this route and makes the file look like it still relies on abstractions that were bypassed in the new implementation.

- [ ] agent permission updates are still stale after first use because the broker caches the capability engine by agent ID and never invalidates it. The cache is established in agent-message-broker.ts, while the API now updates and re-derives toolPolicy in agents.ts. After an agent has made one tool call, later PATCH changes to skillIds or toolPolicy can be stored successfully but remain unenforced until the worker restarts.

- [ ] Sandbox enforcement on code execution has only been partially implemented. SandboxEnforcer and sandbox-exec.sh exist, and code_execute is in the capability grants, but no broker handler routes code_execute calls — the capability is defined but not wired on the production path

- [ ] the production guardrails are still effectively untested at the loader level. The guard was added in config.ts and config.ts, but there is no API config test file and the existing worker config suite in config.test.ts has no coverage for `NODE_ENV=production` with `primaryProvider=mock`. Given this is safety logic intended to prevent a fail-open billing setup, it deserves an executable regression test in both loaders.

- [ ] Plan quota TOCTOU - Plan quotas are still bypassable under concurrency because every new limit check is a read-then-write sequence with no atomicity. The new guards in plan-guards.ts, plan-guards.ts, plan-guards.ts, plan-guards.ts, and plan-guards.ts run before inserts in endpoints like accounts.ts, credentials.ts, instances.ts, and backtests.ts. Two parallel requests can both observe “under limit” and both insert, so users can exceed every new plan cap. This needs a transactional counter/locking strategy or DB-enforced quota model; the current application-side checks are advisory only. WE SKIPPED EARLIER BECAUSE: the read-then-insert pattern in each create route is advisory-only under concurrency. Fixing it correctly requires wrapping every `checkXxxLimit` + `db.insert(...)` in a shared transaction with per-user serialization (advisory lock or `FOR UPDATE`), which is a cross-cutting change across 6 routes and all guard function signatures. This is a known limitation; for a soft billing limit the business impact of one extra row under a rare race is low.

- [ ] Checkout failover still has one unscoped cross-provider lookup. In billing.ts, the route derives the selected interval with resolveIntervalFromPriceId before handing off to the provider manager, but the helper in billing.ts searches Stripe first and then Creem without taking the owning provider. If two providers ever reuse the same ID string for the same plan but different intervals, the fallback path can preserve the wrong interval and create the wrong subscription variant. This should be scoped the same way as the other provider-aware helpers.

- [ ] There is still no regression test for the selected-price failover path, which is why the issue above is currently unprotected. The fallback coverage in provider-manager.test.ts exercises only the no-price case; it does not assert that a caller-selected month or year variant survives a primary-provider outage. Adding one test there would make this class of bug much harder to reintroduce.

- [ ] Consider getting HYPERLIQUID_TESTNET_API_KEY and setting it, to enable the related integration tests. 

- [ ] After monorepo scaffold: add `eslint-plugin-boundaries` if deep-path imports across packages become a recurring review issue. Until then, pnpm workspace resolution + clean barrel exports (`src/index.ts`) enforce dependency direction at build time.

- [ ] After monorepo scaffold: move §21 Conventions (Result type, error code naming) from the design doc into `packages/domain/README.md` or a top-level `docs/conventions.md` — somewhere that lives next to the code, not buried in a feature proposal.

- [ ] When implementing venue adapters: apply rate-limiting lessons from `docs/lessons/rate-limiting-guide.md` — never nest rate-limited calls, short TTL for empty/error cache entries, staleness max on cached prices, self-healing pressure backoff, and check whether provider limits are per-IP or per-key before sharing counters.

- [ ] Get insights from: /Users/chinomso.ikwuagwu/dev_ai/aitradingbot/config.example.yaml. Is there anything you would add or change? Now or later?

- [ ] accounts.test.ts:61, accounts.test.ts:67, and accounts.test.ts:190 use vi.spyOn(HyperliquidAdapter, 'probe') in multiple tests, but the suite only calls vi.clearAllMocks(). That clears call history, not mocked implementations, so one test’s probe stub can leak into later tests and mask route regressions. What to change: switch the setup to vi.restoreAllMocks() or restore these spies explicitly in afterEach. Dependency: none. Risk: low production risk, medium test-suite reliability risk. Test coverage: no new integration coverage needed; this is a unit-test isolation fix.

- [] [low] Add an interaction-level test for the create flow in AgentsPage.tsx:418, AgentsPage.tsx:507, and AgentsPage.tsx:569. The current coverage proves helper behavior and static rendering, but it still does not exercise the actual review/create button wiring that blocks invalid tick intervals. Dependencies: none. Risks/open questions: none beyond deciding whether this belongs in a component-level integration test or a higher-level UI test. Test guidance: integration test, not unit; no visual verification needed.

- [ ] [low] Clarify the UX for preserved legacy cadences around EditAgentModal.tsx:67, EditAgentModal.tsx:91, and AgentControlsSection.tsx:46. The current implementation is functionally correct, but for legacy values like 30_000 ms the editable field still shows 1 while the preview says every 30s. The notice explains this, so I would not block on it, but the control still reads like an exact value rather than an approximation. Consider rendering the field as explicitly approximate until the user edits it, or using a separate read-only legacy badge plus a “normalize” affordance. Dependencies: none. Risks/open questions: this is a UX tradeoff, not a correctness issue. Test guidance: visual verification is the main need; a render test can lock the chosen wording once finalized.

- [ ] [low] If you want stronger defense-in-depth, replace the generic throw in agent-payloads.ts:4 with a typed validation result or a dedicated invariant wrapper. Right now the behavior is acceptable because the only callers are already guarded by field-level validation, but a future caller that bypasses those guards will surface a generic client-side exception path rather than a typed validation failure. Dependencies: none. Risks/open questions: whether you want these UI payload builders to remain fail-fast helpers or evolve toward explicit validation results. Test guidance: unit tests are sufficient if you change this.
